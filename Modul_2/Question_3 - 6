Question 3. How many rows are there for the Yellow Taxi data for all CSV files in the year 2020?
following the code :
select count(yellow_tripdata.unique_row_id) as Jumlah_rows from public.yellow_tripdata;
answers >>> 24.648.499 rows

Question 4. How many rows are there for the Green Taxi data for all CSV files in the year 2020?
following the code :
select count(green_tripdata.unique_row_id) as Jumlah_rows from public.green_tripdata;
answers >>> 1,734,051 rows

Question 5. How many rows are there for the Yellow Taxi data for the March 2021 CSV file?
following the code :
SELECT COUNT(yellow_tripdata.unique_row_id) AS jumlah_rows
FROM public.yellow_tripdata
WHERE yellow_tripdata.filename = 'yellow_tripdata_2021-03.csv';
answers >>> 1,925,152 rows

Question 6. How would you configure the timezone to New York in a Schedule trigger?
following the documentation:
https://kestra.io/docs/workflow-components/triggers/schedule-trigger
answers >>> Add a timezone property set to America/New_York in the Schedule trigger configuration
